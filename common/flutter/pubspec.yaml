name: mvp_flutter_common
description: Shared Flutter upper-computer communication and UI layer.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.0
  flutter_bluetooth_serial:
    path: third_party/flutter_bluetooth_serial_compat
  mqtt_client: ^10.11.1

flutter:
  uses-material-design: true
